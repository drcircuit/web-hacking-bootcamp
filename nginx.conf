user  nginx;
worker_processes  auto;

error_log  /var/log/nginx/error.log notice;
pid        /var/run/nginx.pid;

events {
    worker_connections  1024;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    # DNS resolver (uses Docker's internal DNS)
    resolver 127.0.0.11 valid=30s;

    log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
                      '$status $body_bytes_sent "$http_referer" '
                      '"$http_user_agent" "$http_x_forwarded_for"';

    access_log  /var/log/nginx/access.log  main;

    sendfile        on;
    keepalive_timeout  65;

    # Bootcamp Portal
    server {
        listen 80;
        server_name bootcamp.local;
        location / {
            proxy_pass http://bootcamp-portal:8080; # Changed from :80 to :8080
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        }
    }

    # Lab 1 Target
    server {
        listen 80;
        server_name lab1.evilcorp.local;

        location / {
            proxy_pass http://lab1-recon-box:80;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        }
    }

    # Lab 2 Target
    server {
        listen 80;
        server_name lab2.evilcorp.local;

        location / {
            proxy_pass http://lab2-access-box:80;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        }
    }
    # Lab 3 Target
    server {
        listen 80;
        server_name lab3.evilcorp.local;

        location / {
            proxy_pass http://lab3-crypto-box:80;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        }
    }
    # Lab 4 Target
    server {
        listen 80;
        server_name lab4.evilcorp.local;

        location / {
            proxy_pass http://lab4-injection-box:80;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        }
    }
}